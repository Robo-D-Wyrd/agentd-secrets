{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agent-secretd.fullname" . }}-secrets
  labels:
    {{- include "agent-secretd.labels" . | nindent 4 }}
type: Opaque
data: {}
  # Populate via: helm install --set-literal existingSecret=my-secret
  # Or create the secret with agent-secretd-admin:
  #   bin/agent-secretd-admin.py create-secret \
  #     --keycloak-client-secret '...' \
  #     --keycloak-password '...' \
  #     --generate-enc-key
  # Required keys: KEYCLOAK_CLIENT_SECRET, WRAPTOKEN_ENC_KEY, KEYCLOAK_USERNAME, KEYCLOAK_PASSWORD
{{- end }}
