apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-secretd.fullname" . }}
  labels:
    {{- include "agent-secretd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-secretd.selectorLabels" . | nindent 4 }}
