apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agent-secretd.fullname" . }}-test-connection"
  labels:
    {{- include "agent-secretd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '{{ include "agent-secretd.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
